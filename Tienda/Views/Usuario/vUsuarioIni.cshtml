@model List<clsUsuario>

@{
    ViewData["Title"] = "vUsuarioIni";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="text-center">
    <h1 class="display-4">Usuarios</h1>
    @*<p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>*@
</div>

<div class="card">
    <div class="card-header">
        Listado de Usuarios
    </div>
    <div class="card-body">

        <a asp-action="vUsuarioDet" asp-controller="Usuario" asp-route-_IdUsuario="0" class="btn btn-success btn-sm">Nueva Usuario</a>
        @*<a asp-action="test" asp-controller="Home" class="btn btn-success">test</a>*@
        <hr />
        <table class="table">
            <thead>
                <tr>
                    <th>Nombre</th>                    
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.strUserName</td>
                        <td>
                            <a asp-action="vUsuarioDet" asp-controller="Usuario" asp-route-_IdUsuario="@item.UserId" class="btn btn-primary btn-sm">Editar</a>
                            <a asp-action="Eliminar" asp-controller="Usuario" asp-route-_IdUsuario="@item.UserId" class="btn btn-danger btn-sm">Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

</div>